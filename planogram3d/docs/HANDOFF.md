# База знаний разработки (handoff для продолжения на локальной копии)

*Всё, что накоплено за облачную сессию разработки planogram3d:
состояние, архитектурные решения с причинами, известные грабли,
рецепты тестирования, бэклог. Файл читается человеком и ИИ-ассистентом;
вместе с `../../CLAUDE.md` он переносит контекст сессии на любую
машину, куда вытянута ветка.*

Рабочая ветка: `claude/3d-sales-visualization-fsbyam`.

---

## 1. Что уже существует и работает

| Раздел | Маршрут | Проверено |
|---|---|---|
| 3D-планограмма (2 режима + отчёт соответствия) | `/store/<id>`, CLI `python -m planogram3d` | запуском + скриншоты |
| Карта сети 2D/3D (Тверь, OSM), эмуляция дня | `/` | да |
| Zabbix-бейджи (реальный API / эмулятор) | `/` карточки | API протестирован на мок-сервере |
| Логистический центр + грузовики (опция `PLANOGRAM_DC`) | `/` маркер РЦ | да |
| Вход в магазин (полёт камеры) + 3 вкладки | интерьер | да |
| Симуляция зала: очереди, ТСД BLE/LoRa, холодильники, расходники, скоропорт, оценка трафика | `/store/<id>/live` | да, включая реальный ingest |
| Тренажёр соло (3 смены) | `/store/<id>/game` | автопрогон Playwright |
| Командная смена ≤10 игроков + ИИ-боты + внешние агенты | `/store/<id>/game/multi` | лимиты, боты, внешний агент — всё прогнано |
| Roblox: баллы/бейджи, Open Cloud / эмулятор | `/team`, API | эмулятор прогнан; Open Cloud написан по споке, не проверен без ключей |
| Конвертер сцен → .rbxlx + Luau-тренажёр | `python -m planogram3d.robloxkit` | XML валиден; **в Roblox Studio не открывался** (нет Studio в контейнере) |
| Доставка: сборка→маршруты→Гант→GPS→чеки 54-ФЗ | `/delivery`, `/receipt/<id>` | да, включая реальный GPS ingest |
| ИИ-табло прибытия для пунктов доставки (магазины и адреса) | карточка магазина на `/`, попап точки на `/delivery`, `/api/eta/*` | да, запуском + скриншоты; модель учится (obs растёт, скорость уходит от априорной) |
| Документация в HTML + презентация с BPMN | `/docs/`, `/presentation` | все ссылки прочеканы скриптом (200) |
| Нативное окно (pywebview) | `python -m planogram3d.desktop` | запущено в контейнере под Xvfb, скриншот окна снят |
| Лаунчеры/установщик macOS | `PlanogramApp.command`, `Установить_Planogram3D.command` | установщик прогнан в чистом HOME сквозняком |

Запуск: `python3 -m planogram3d.webapp` (веб, :8050) ·
`python3 -m planogram3d.desktop` (нативное окно) ·
`python3 -m planogram3d` (CLI-отчёт).

## 2. Карта кода

```
planogram3d/
  core/            ядро, 853 строки, зависимость только plotly
    models.py      Store и вся модель данных (dataclasses)
    rules.py       проверки: регламент/контракты/план-факт/продажи + extra_checks
    viz.py         Plotly-сцены: cuboid, edges_trace, build_figure
    report.py      build_report_page / save_report / render_*_report
  webapp/          Flask + ванильный JS
    network.py     эмуляция сети, DistributionCenter, price_for, NETWORK_LAYOUT
    instore.py     зал: события, ТСД, холодильники, расходники, очереди, призраки
    multigame.py   командная смена: роли/лимиты, боты, авторитарный тик
    delivery.py    заказы→маршруты→GPS→фискальные чеки
    eta_ai.py      ИИ-прогноз прибытия (онлайн-EWMA: скорость, трафик
                   по часам, вручение); общая модель РЦ+курьеров
    zabbix.py      ZabbixClient (Open Cloud НЕ путать) + эмулятор
    roadnet.py     дорожный граф из OSM GeoJSON, A*-маршруты,
                   point_along; курьеры и грузовики едут по улицам
    roblox.py      TeamHub: register/progress/award, Open Cloud/эмулятор
    mdview.py      мини-Markdown → HTML (см. грабли §4.7)
    server.py      все маршруты; _DOCS — навигация документации
    templates/     map, instore, game, game_multi, delivery, receipt,
                   presentation (BPMN-SVG внутри), docs, team
    static/        vendor/maplibre-gl.*, data/tver_map.geojson (автономная карта)
    tools/         build_map_data.py (Overpass→GeoJSON), demo_bot.py
                   (эталонный внешний ИИ-агент), build_article_html.py
  robloxkit/       rbxlx.py (писатель формата), luau.py, convert.py, demo/
  desktop.py       pywebview-обёртка; PLANOGRAM3D_NO_GUI=1 — headless-тест
  docs/            статьи, методичка, роадмап, презентации, img/
```

## 3. Архитектурные решения и их причины

1. **Один объект `Store` — вся интеграция.** Любые данные (ERP, CSV,
   БД) заходят только через него; ядро не знает об источниках.
2. **Эмулятор и реальный источник — один интерфейс.** Zabbix, события
   зала (`/api/instore/<id>/ingest`), GPS доставки, Roblox Open Cloud:
   везде провайдер выбирается по env-переменным, реальный поток
   глушит эмулятор по таймауту (60 с зал, 45 с GPS). Это позволило
   демонстрировать всё с первого дня.
3. **Evolve-on-poll вместо фоновых потоков** в instore/delivery/
   multigame: мир «доигрывается» при каждом запросе состояния (dt
   ограничен). Исключение — network: фоновый тикер-поток (2 с) и
   таймскейл 1 с = 10 мин. Не плодить новых потоков без нужды.
4. **Командная смена авторитарна на сервере**, клиенты тонкие; роль не
   знает, кто её исполняет (human/bot/api) — это фундамент роадмапа
   ИИ-замещения (docs/ROADMAP_AI_WORKFORCE.md).
5. **Карта полностью автономна**: MapLibre и GeoJSON Твери лежат в
   репозитории; интернет для работы не нужен. Пересборка под другой
   город — `webapp/tools/build_map_data.py` из выгрузок Overpass.
6. **Отчёт планограммы** по умолчанию `include_plotlyjs=True`
   (автономный файл ~5 МБ); для веб-отдачи есть `"cdn"` (~250 КБ).
7. **RBXLX пишем сами** (без зависимостей): свойства сериализуются
   вручную, токены Enum.Material захардкожены в `rbxlx.py`.
8. **Мини-Markdown свой** (`mdview.py`), а не библиотека — ноль
   зависимостей; поддержано: заголовки, списки, таблицы, код, картинки,
   склейка мягких переносов.

## 4. Известные грабли (сэкономят часы)

1. **`pkill -f planogram3d` убивает сам вызывающий bash** (паттерн
   совпадает с командной строкой). Убивать сервер только так:
   `fuser -k 8050/tcp`.
2. **Playwright не может кликнуть анимированный маркер** (元 element
   is not stable из-за pulse-анимации) — кликать через
   `page.evaluate("...click()")`.
3. **pywebview не ставится системным pip на Ubuntu** (патченый
   setuptools ломает сборку proxy_tools: `install_layout`). В venv
   ставится нормально — лаунчеры и установщик работают через venv.
4. **PyGObject (gi) привязан к версии питона**: в контейнере `_gi` был
   собран под 3.12, а дефолтный python — кастомный 3.11 → venv строить
   от `/usr/bin/python3.12 --system-site-packages`. На Mac эта проблема
   отсутствует (pywebview использует pyobjc).
5. **Overpass API часто отдаёт 504/обрывы** — делить запрос на части
   (roads/buildings/nature), ретраи с паузами, зеркало
   overpass.kumi.systems. Готовые данные уже в репо — без нужды не
   пересобирать.
6. **LibreOffice в облачном контейнере не открывает pptx вообще**
   («source file could not be loaded» даже на минимальном файле) —
   визуальную проверку pptx делать нечем; валидируется скриптом
   validate.py и python-pptx, финальную проверку делать локально в
   PowerPoint/Keynote.
7. **mdview._unwrap**: исходники MD с жёсткими переносами склеиваются
   в абзацы; была ошибка «каждая строка — отдельный `<p>`». Если
   добавляете новую Markdown-конструкцию — проверяйте взаимодействие
   со склейкой (код-фенсы исключены из склейки).
8. **`price_for()` использует `hash(sku)`** — из-за рандомизации хэша
   Python цены МЕНЯЮТСЯ между перезапусками процесса. Для демо
   терпимо, для стабильных тестов — заменить на
   `zlib.crc32(sku.encode())` (см. бэклог).
9. **Эмодзи в headless Chromium** рендерятся упрощённо (моно-глифы) —
   это нормально для скриншотов, на живых машинах вид богаче.
10. **Порт 8050 занят после падения** — снова `fuser -k 8050/tcp`;
    сервер запускать через `setsid nohup ... &`, иначе умрёт с шеллом.
11. **Один тик эмуляции сети продаёт помногу** — если менять
    `TIME_SCALE`/скорости, перепроверять экономику смен тренажёра
    (цели подобраны под текущие константы).

## 5. Рецепты тестирования (проверено в этой сессии)

* **Smoke всей системы**: поднять сервер, `curl /api/state`,
  `/api/instore/st17/state`, `/api/delivery/state`,
  `/api/roblox/team` — все должны отдавать 200 и живые числа.
* **Скриншоты экранов** — Playwright + системный Chromium
  (`executable_path='/opt/pw-browsers/chromium'` в контейнере; локально
  обычный `playwright install`). Двухкратный `device_scale_factor` даёт
  чёткие снимки для доков.
* **Игровые сценарии** прогоняются через `window.game`-хуки:
  `game.clickHall(x, y)` в соло, REST-действия в командной смене.
  Быстрые прогоны: `?time=30&level=2` у соло-тренажёра.
* **Линк-чекер презентации**: собрать `href` со страницы `/presentation`
  регуляркой и обойти curl'ом — все внутренние ссылки должны быть 200.
* **Установщик** тестировать в чистом `HOME` (подменой env), с
  `PLANOGRAM3D_NO_GUI=1`.
* **Внешний ИИ-агент**: `python -m planogram3d.webapp.tools.demo_bot
  --role cashier --room тест --start --seconds 60` — должен набрать
  баллы больше нуля.

## 6. Переменные окружения (все опциональны)

| Переменная | Назначение |
|---|---|
| `PLANOGRAM_DC=0` | отключить логистический центр |
| `ZABBIX_URL`, `ZABBIX_TOKEN`, `ZABBIX_HOSTS` | реальный Zabbix вместо эмулятора |
| `ARTGRANIT_URL` | адрес ERP Artgranit (например `http://127.0.0.1:3003`); не задана — топливный контур (`peco_fuel.py`) полностью автономен, ни одного сетевого вызова |
| `ARTGRANIT_TIMEOUT=3.0` | таймаут одного GET-запроса к Artgranit, с |
| `ARTGRANIT_POLL_INTERVAL=5.0` | не опрашивать Artgranit чаще этого интервала (evolve-on-poll, без потока) |
| `REAL_ARTGRANIT_TIMEOUT=20.0` | окно свежести реальных данных после последнего успешного опроса — истекло, контур сам возвращается к эмуляции (образец — `REAL_GPS_TIMEOUT` в `delivery.py`) |
| `ROBLOX_API_KEY`, `ROBLOX_UNIVERSE_ID` | реальный Open Cloud (DataStore + MessagingService) |
| `PLANOGRAM_TEAM_FILE` | путь файла-реестра команды (default `planogram_team.json` в cwd) |
| `PLANOGRAM3D_NO_GUI=1` | desktop.py: только сервер, без окна (для CI) |

## 7. Документы и артефакты

`docs/`: TZ (техзадание системы, критерии приёмки), LIBRARY (API),
INTEGRATION (все внешние API с примерами),
ARTICLE_3D_ERP (позиционирование), ARTICLE_TEAM_TRAINING (методичка,
HTML-версия генерится `webapp/tools/build_article_html.py` — не
редактировать HTML руками), ROADMAP_AI_WORKFORCE (уровни A0–A5),
PRESENTATION_PLAN + presentation.pptx, ARTICLE_CLOUD_CONTAINER,
img/ (скриншоты), robloxkit/demo/ (готовые .rbxlx).

## 8. Бэклог (идеи, до которых не дошли руки)

1. Стабилизировать цены: `hash()` → `crc32` в `price_for` (§4.8).
2. Agent API v1: версионирование, токены, сид-режим и «экзамен роли»
   (фаза 1 роадмапа ИИ).
3. Открыть demo .rbxlx в настоящей Roblox Studio и починить, если
   Studio к чему-то придерётся (не проверялось, §1).
4. ~~Маршруты по дорогам~~ — СДЕЛАНО: `webapp/roadnet.py` (граф 5115 узлов, 95% связности, A* за ~2 мс); курьеры и грузовики РЦ едут по полилиниям улиц, линии маршрутов на картах — дорожные.
5. ~~Собрать настоящий .app~~ — СДЕЛАНО: бандл `Planogram3D.app` в
   корне репозитория (Info.plist + icns-иконка, лаунчер через venv
   `.venv-planogram3d`, двойной клик без терминала); по желанию —
   py2app-сборка со встроенным питоном.
6. Тесты pytest на ядро (rules) — сейчас всё проверено сценарно.
7. Продовый WSGI (waitress/gunicorn) вместо dev-сервера Flask.
8. В командной смене: голосовая/чат-коммуникация, больше событий.
